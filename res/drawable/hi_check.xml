<?xml version="1.0" encoding="utf-8"?>
<!-- 
.checkbox { border: 1px solid #D4D4D4;
			border-radius: 2px;
			background: white; }
.checkbox.high { border-color: #df0505;
				 background: -webkit-gradient(linear, 50% 0%, 50% 100%, color-stop(0%, #fd9a9a), color-stop(100%, #fdbdbd));
				 background: -webkit-linear-gradient(top, #fd9a9a 0%, #fdbdbd 100%);
				 background: -moz-linear-gradient(top, #fd9a9a 0%, #fdbdbd 100%);
				 background: -o-linear-gradient(top, #fd9a9a 0%, #fdbdbd 100%);
				 background: -ms-linear-gradient(top, #fd9a9a 0%, #fdbdbd 100%);
				 background: linear-gradient(top, #fd9a9a 0%, #fdbdbd 100%); }
-->
<!-- LOW -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Checked -->
    <item android:state_checked="true"
        android:drawable="@drawable/logged" />
    
    <!-- Checked & Pressed, TODO -->
    <item android:state_checked="true" android:state_pressed="true"
        android:drawable="@drawable/logged"
        android:background="#cc000000" />
    
    <!-- Unchecked -->
	<item>
		<shape android:shape="rectangle" >
             <gradient
                android:startColor="#fdbdbd"
                android:endColor="#fd9a9a"
                android:angle="90" />
		    <stroke android:width="2dp" android:color="#df0505" />
		    <size android:height="30dp" android:width="30dp" />
            <corners
                android:radius="5dp" />
		</shape>
    </item>
</selector>